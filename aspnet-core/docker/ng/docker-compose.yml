version: '2'

services:
            
    mrpanel_host:
        image: mrpanel_host
        environment:
            - ASPNETCORE_ENVIRONMENT=Staging
        ports:
            - "9901:80"
        volumes:
            - "./Host-Logs:/app/App_Data/Logs"

    mrpanel_ng:
        image: mrpanel_ng
        ports:
            - "9902:80"
            
    mrpanel_ng_site:
        image: mrpanel_ng_site
        ports:
            - "9903:80"            